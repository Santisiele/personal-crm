# Base URL of the CRM API. In development the API runs on its own origin
# (Nest on :3000), so point the SPA at it. In production the SPA is served by
# Nest at the same origin under /app, so leave this empty to call the API at the
# root of the current origin.
VITE_API_URL=http://localhost:3000
